# Local / OS junk (not needed for install/build)
.env
.env.*
!.env.example
node_modules
.tsbuildinfo
.DS_Store

# Large / unused for the Vercel function + static deploy
attached_assets
scripts
**/*.md
!replit.md

# Do NOT ignore artifacts/api-server/dist — buildCommand creates
# dist/vercel.mjs and api/index.js imports it.
