# Local / OS junk (not needed for install/build)
.env
.env.*
!.env.example
node_modules
.tsbuildinfo
.DS_Store

# Large / unused for the Vercel function + static deploy
attached_assets
**/*.md
!replit.md

# Keep scripts/ — buildCommand runs scripts/prepare-vercel-api.mjs
# Keep api/index.js — handler. Generated api/*.mjs are created at build time.
